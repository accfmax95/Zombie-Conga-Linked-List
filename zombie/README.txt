Put the makefile and the termcolor.hpp in the directory
shared by the .cpp and .h files. Used the command "make"
to create the executable in general.asu, and then run
./Assignment4 to start the program.